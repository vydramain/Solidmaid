# DEVLOG — HUD, Locomotion Feel & Hitstop System

---

## Date: 2025-11-16

### 🎯 Goal of the Day
> Довести FPS прототип до M1: читаемый HUD, живой контроллер (head bob + micro-shake) и базовый hitstop, чтобы кнопки ощущались как удары.

---

### 🧩 Tasks Executed
- [x] Переписал HUD (CanvasLayer + `hud.gd`): HP, способности, руки, скорость/направление, реагирует на Interactor и масштабируется pixel-perfect.
- [x] Ввел VisionRig компонент: head bob зависит от скорости Locomotion, micro-shake вызывается через `trigger_micro_shake()`.
- [x] Настроил значения боба (амплитуды, 5.8 Hz, плавное сглаживание) → лёгкий бег не укачивает.
- [x] Locomotion теперь использует acceleration/deceleration, так что движение разгоняется/тормозит как у человека (12/16 м/с²).
- [x] Добавил hitstop систему (`resources/systems/hitstop/HitstopSystem.tscn` + autoload): временно снижает `Engine.time_scale`, может трясти камеру.
- [x] Контроллер вызывает hitstop/shake при атаках, бросках, взаимодействиях — кнопки сразу чувствуются весомее.
- [x] Devlog/GDD сверка: отметили, что из M1 теперь закрыты и hitstop, и micro-shake.

---

### 💡 What Worked
- DI вокруг Character → HUD → VisionRig упростил подключение: контроллер автонаходит HUD/rig, сцена остаётся чистой.
- Walk bob + inertia делают даже пустой Sandbox приятнее: камера слегка качается, движение не резкое.
- Hitstop как отдельная система легко переиспользовать, не привязываясь к конкретной способности/оружию.

---

### ⚙️ Issues / What Didn’t Work
- Сначала сделал hitstop скриптом с `data class` — GDScript 4.5 такое не ест; переписал на простые свойства.
- Старые VisionRig амплитуды укачивали на 1280×800; пришлось уменьшить смещения и сделать более медленную частоту.

---

### 🔁 Next Steps / TODO
- [ ] HUD: индикаторы кулдауна, простая HP bar, toggle для скрытия debug-блоков.
- [ ] Подцепить hitstop к реальным попаданиям/урону (сейчас триггерится на кнопках).
- [ ] Добавить stamina/ritual прогресс, чтобы HUD перестал быть сугубо debug.

---

### 🗃️ Commit Summary (high-level)
- Feature: HUD telemetry, VisionRig head bob/micro-shake, locomotion inertia, hitstop system.
- Polish: pixel-perfect CanvasLayer, комфортные настройки камеры и движение, автозапуск hitstop из контроллера.
