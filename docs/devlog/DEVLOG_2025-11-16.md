# DEVLOG — HUD Telemetry & Pixel-Perfect Scaling

---

## Date: 2025-11-16

### 🎯 Goal of the Day
> Превратить FPS HUD в полезный слой телеметрии (HP, руки, способности, движение) и добиться читаемости текста при растянутом viewport-е.

---

### 🧩 Tasks Executed
- [x] Перенёс CanvasLayer в `resources/ui/HUD.tscn`, добавил секции Vitality/Abilities/Carries/Locomotion с placeholder-лейблами.
- [x] Дописал `hud.gd`: бинды к `Character`, Vitality, CarrySlots, Locomotion, Interactor; обновление лейблов и ability-списка в рантайме.
- [x] Прокинул `get_vitality()` в `character.gd` и подключил HUD в `ControllerForPlayer`, чтобы CanvasLayer появлялся автоматически с игроком.
- [x] Реализовал pixel-perfect масштабирование: HUD читает `viewport_width/height`, вычисляет integer scale от текущего окна и компенсирует позицию, чтобы текст не замыливался.

---

### 💡 What Worked
- DI между Character → HUD сделало сцену самодостаточной: достаточно инстансить контроллер, и UI сразу тянет данные без ручных ссылок.
- Integer scaling + компенсация остатка устранили микроразмазывание текста при растяжении из 256×160 → 1280×800.
- Список способностей и подписи рук помогают дебажить CarrySlots/Ability flow без логов.

---

### ⚙️ Issues / What Didn’t Work
- Строгий GDScript ругался на вывод типов (`ProjectSettings.get_setting`), пришлось явно указывать Variant.
- Сначала HUD перехватывал события мыши и ломал look input; теперь все Control-узлы стоят в `MOUSE_FILTER_IGNORE` и не мешают.

---

### 🔁 Next Steps / TODO
- [ ] Добавить визуальные индикаторы кулдауна броска и HP бар вместо чисел.
- [ ] Подключить HUD к future stamina/ritual прогрессам (placeholder уже есть).
- [ ] Спрятать debug-метрики за toggle, чтобы не мозолили глаза в обычной сборке.

---

### 🗃️ Commit Summary (high-level)
- Feature: HUD, который отображает HP, руки, способности, состояние движения и реагирует на Interactor.
- Tech: Pixel-perfect масштаб CanvasLayer через integer scale и учёт viewport stretch.
