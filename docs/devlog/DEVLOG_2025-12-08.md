# DEVLOG — Body Component & Player Appearance Hookup

---

## Date: 2025-12-08

### 🎯 Goal of the Day
> Научить сущность игрока подгружать любую GLB-модель через отдельный компонент и убедиться, что SandBox.scn использует PlayerAppearance.

---

### 🧩 Tasks Executed
- Добавлен `BodyComponent` (`resources/overlap/appearance/body_component.gd/.tscn`) — экспортирует `body_scene` и инстанцирует PackedScene с проверкой на `Node3D`, чтобы визуал игрока стал обычным компонентом.
- В `Character.tscn` подключён новый компонент и помечен editable, поэтому любой инстанс персонажа может подставить свой внешний вид без правки логики.
- В `Sandbox.tscn` установлено значение `body_scene`, теперь уровень подмешивает `PlayerAppearance.tscn`, а не голый GLB; можно редактировать его прямо из уровня.
- Создан мини-гайд `docs/howto/add_player_body_model.md`, описывающий импорт GLB → настройку BodyComponent → подстройку трансформов.

---

### ⚙️ Issues / What Broke
- `PlayerAppearance` использует GLB без смещения/масштаба; пока не будет выровнен относительно капсулы и камеры, модель в редакторе может находиться вне кадра или под землёй.
- Остальные сцены с `Character` по-прежнему пусты — нужно вручную задать `body_scene`, иначе персонаж остаётся невидимым.

---

### 🔁 Next Steps / TODO
- [ ] Подогнать позицию/масштаб `PlayerAppearance` (и будущих GLB) под `CollisionShape3D` капсулы и `VisionRig`, чтобы модель гарантированно попадала в кадр.
- [ ] Добавить hurtbox/урон в `MeleeAbility`, чтобы удар трубой наносил эффект.
- [ ] Доработать HUD/инпут, чтобы показывать статусы слотов (что в руках, готово ли действие).
- [ ] Подключить больше предметов к affordance системе (например, будущие melee/throw объекты) и проверить кросс-комбинации ability.
- [ ] Починить melee-анимацию для трубы (pipe): убедиться, что swing проигрывается корректно в руках.
- [ ] Починить баг: труба при взятии в руки падает под карту — зафиксировать позицию/физику при подъёме.
- [ ] Протестировать модификатор дропа (G/D-pad Down + рука) и уточнить поведение при пустых слотах/отсутствии affordance.
- [ ] Ввести HandAnimator/AnimationTree для рук: базовые состояния (idle, pickup, drop, throw_release, melee_swing), разделить FP/TP клипы и определить место хранения (общая библиотека + профили предметов).
- [ ] Добавить реальные клипы для pipe (fp/tp), настроить AnimationTree/state machine, обвязать hurtbox-триггеры в анимациях.
- [ ] Проверить сцены/сигналы на сломанные ссылки после переименований и UID-фиксов.

---

### 🗃️ Commit Summary
- Внедрён BodyComponent, обновлены `Character.tscn`/`Sandbox.tscn`, и задокументирован процесс подключения GLB-моделей через новый компонент.
