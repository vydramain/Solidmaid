# DEVLOG — FPS Controller Polish & Docs Sync

---

## Date: 2025-11-10

### 🎯 Goal of the Day
> Довести до рабочего состояния базовый FPS-контроллер (движение+камера) и синхронизировать документацию/лог, чтобы отражать новый пайплайн.

---

### 🧩 Tasks Executed
- [x] Перенёс `Character.tscn` на `Locomotion` в качестве корня, чтобы трансформы игрока совпадали с CharacterBody3D.
- [x] Привёл `character.gd` к наследованию от `locomotion.gd`, сохранив DI-компоненты (здоровье, способности, interactor, контроллер).
- [x] Обновил `ControllerForPlayer.gd`: управление захватом курсора (Esc отпускает, клик/фокус возвращает), yaw/pitch обрабатываются только при захваченном курсоре.
- [x] Завёл placeholder `ControllerForAI.gd` и сцену, чтобы слот мог переключаться без ошибок.
- [x] Настроил input map (`project.godot`) для FPS (`move_forward/backward`, `attack`, `throw`, `interact`) параллельно с legacy-действиями.
- [x] Создал `resources/objects/sandbox/Sandbox.tscn` для быстрого теста движения/взгляда.
- [x] Дописал README: отдельный блок про FPS V2, инструкции по управлению, как запускать Sandbox, отражены новые директории.
- [x] Составил devlog и зафиксировал текущий статус M1 (ещё остаются UI, throw, shake).

---

### 💡 What Worked
- Перенос на `Locomotion` устранил рассинхрон позиций: теперь камера и interactor “едут” вместе с телом.
- Захват курсора + восстановление по фокусу решают UX проблему (можно alt-tab’ить, Esc временно показывает мышь).
- Sandbox позволяет воспроизводимо чекать look/move без legacy сцены и без clutter.

---

### ⚙️ Issues / What Didn’t Work
- Interactor пока без визуального UI/подсказки — только вызывает `interact(by)`.
- `AbilityToThrow`/`AbilityToMelee` всё ещё заглушки; нет кирпича/кулдаунов, поэтому M1 не закрыт полностью.
- Нет crosshair/head bob/camera shake, как требовалось в M1.

---

### 🧠 Lessons / Insights
- Делать CharacterBody3D корнем сильно упрощает композицию и работу с трансформами — меньше синхронизаций и багов с камерой.
- Даже FPS-прототипу нужна отдельная песочница: экономит время на запуске legacy уровней и быстрее выявляет UX проблемы (курсор, чувствительность).

---

### 🔁 Next Steps
- Добавить UI crosshair + подсказку взаимодействия; повязать на Interactor сигналы.
- Реализовать бросок кирпича (RigidBody3D + Ability wrapper) и простой кулдаун.
- Лёгкий head bob и микрошэйк камеры по тактам атаки/урона, чтобы закрыть пункты M1.
- Добавить хотя бы один тестовый interactable объект в Sandbox.

---

### 🗃️ Commit Summary (high-level)
- Refactor: Character now inherits Locomotion; controller slots updated; player controller обрабатывает курсор/фокус; input map расширен.
- Feature: Sandbox scene, AI controller stub, documentation/devlog refresh.
- TODO: Throw ability, UI crosshair/prompt, camera feedback.
