# DEVLOG — Hand Interaction Routing & Pipe Polish

---

## Date: 2025-11-19

### 🎯 Goal of the Day
> Совместить архитектурное описание overlaps с фактическим кодом: руки должны работать через ability/affordance, а тяжёлые предметы — корректно висеть в слотах.

---

### 🧩 Tasks Executed
- [x] `Character` стал публичным маршрутизатором рук (`class_name Character`). Добавил `interact_hand(slot)` — контроллер больше не вызывает `request_throw()` напрямую, а получает детальный ответ (pickup/throw/melee/interaction).
- [x] Обновил `controller_for_player.gd`: каждое нажатие руки просто вызывает `character.interact_hand()`, а лог/фидбек зависят от типа действия. Ввод больше не знает, какие предметы бросаются, какие бьют, какие подбираются.
- [x] Ввёл реальные ability/affordance скрипты: `ThrowAbility`, `MeleeAbility` (с кулдауном и вызовом анимации), `MeleeAffordance`. Теперь пары ability↔affordance действительно управляют поведением предметов.
- [x] CarrySlots кэширует ability-инстансы и решает, что делать с предметом на основе его affordance (`carriable`, `throwable`, `melee`). Левые/правые слоты снова независимы.
- [x] Труба получила отдельный `pipe.gd`: freeze/disable collision при подъёме, возвращение физики при броске, без принудительного локального трансформа. Теперь предмет остаётся в руках и не исчезает при swing.

---

### ⚙️ Issues / What Broke
- Потребовалось полностью пересобрать `controller_for_player`: прежние `request_throw()` / `pickup` вызовы оказались несовместимы с новой архитектурой, пришлось изменить логику ввода.
- `MeleeAbility` пока только проигрывает анимацию и триггерит обратную связь; hitbox/урон ещё не подключены. Труба визуально работает, но врагов всё ещё бить нельзя.

---

### 🔁 Next Steps / TODO
- [ ] Добавить hurtbox/урон в `MeleeAbility`, чтобы удар трубой наносил реальный эффект.
- [ ] Доработать HUD/инпут, чтобы показывать статусы слотов (что в руках, готово ли действие).
- [ ] Подключить больше предметов к affordance системе (например, будущие melee/throw объекты) и проверить кросс-комбинации ability.

---

### 🗃️ Commit Summary
- `ef445982a` — Carry/ability rebuild: Character теперь сам решает, что делать с предметом в слоте, появились реальные ability/affordance скрипты, труба перестала пропадать из рук.
