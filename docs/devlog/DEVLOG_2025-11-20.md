# DEVLOG — Hand Inputs Only & Docs Sync

---

## Date: 2025-11-20

### 🎯 Goal of the Day
> Убрать абстрактные действия (`interact`/`attack`/`throw`) из ввода и оставить только реальные кнопки рук, чтобы всё поведение решалось ability + affordance.

---

### 🧩 Tasks Executed
- [x] Очистил InputMap: убрал `interact` и прочие абстрактные экшены, оставив только `hand_left`/`hand_right` как единственные кнопки действия.
- [x] Обновил `controller_for_player.gd`: слушает только руки и передаёт intent в `character.interact_hand`, без прямых вызовов `interact()`/`throw()`.
- [x] Синхронизировал документацию (`README`, `arch_overview.md`): описал, что итоговое поведение выбирает связка ability + affordance, а не отдельные хоткеи.

---

### ⚙️ Issues / What Broke
- После удаления `interact` нужно повторно проверить подсказки HUD и sandbox-сцену: всё завязано только на руки, поэтому устаревшие бинды могут молча не работать.

---

### 🔁 Next Steps / TODO
- [ ] Добавить hurtbox/урон в `MeleeAbility`, чтобы удар трубой наносил реальный эффект.
- [ ] Доработать HUD/инпут, чтобы показывать статусы слотов (что в руках, готово ли действие).
- [ ] Подключить больше предметов к affordance системе (например, будущие melee/throw объекты) и проверить кросс-комбинации ability.
- [ ] Починить melee-анимацию для трубы (pipe): убедиться, что swing проигрывается корректно в руках.
- [ ] Починить баг: труба при взятии в руки падает под карту — зафиксировать позицию/физику при подъёме.

---

### 🗃️ Commit Summary
- chore: InputMap очищен от абстрактных действий; контроллер и документация синхронизированы под модель «две руки — один набор кнопок».
