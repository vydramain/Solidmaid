# DEVLOG — Interaction Modifiers & Drop

---

## Date: 2025-11-23

### 🎯 Goal of the Day
> Обновить интеракции рук: добавить модификатор на дроп предметов и переложить логику поднятия/броска на аффордансы, убрав жесткое связывание с конкретными сценами.

---

### 🧩 Tasks Executed
- Убрал анимации Pipe.tscn, оставив пустые стобы, чтобы сцена жила без процедурного свинга.
- Перенес обработку поднятия/броска в `CarriableAffordance`: кэш коллизий, фриз/анфриз, вызовы хостовых хуков.
- В `CarrySlots` добавил `try_drop` и хук-уведомления вместо прямых вызовов методов предмета.
- `Character.interact_hand` теперь принимает модификатор (drop) и возвращает `HAND_ACTION_DROP`, убран `drop_holdable`.
- `controller_for_player` передает модификатор (G или D-pad Down) в интеракцию руки; удален прямой дроп из контроллера.
- В инпутах добавлен `drop_modifier` (G, D-pad Down) для клавиатуры и геймпада.

---

### ⚙️ Issues / What Broke
- Требуется пройтись по предметам, которые раньше завязывались на старые `on_picked_up/on_released`, чтобы убедиться, что им хватает хуков через аффорданс или нужны доп. методы-хосты.
- Не проверялось на железе: нужно тестировать дроп/удар/бросок на геймпаде и клавиатуре, особенно с отсутствием melee/throw affordances.

---

### 🔁 Next Steps / TODO
- [ ] Добавить hurtbox/урон в `MeleeAbility`, чтобы удар трубой наносил реальный эффект.
- [ ] Доработать HUD/инпут, чтобы показывать статусы слотов (что в руках, готово ли действие).
- [ ] Подключить больше предметов к affordance системе (например, будущие melee/throw объекты) и проверить кросс-комбинации ability.
- [ ] Починить melee-анимацию для трубы (pipe): убедиться, что swing проигрывается корректно в руках.
- [ ] Починить баг: труба при взятии в руки падает под карту — зафиксировать позицию/физику при подъёме.
- [ ] Протестировать новый модификатор дропа (G/D-pad Down + рука) и уточнить поведение при пустых слотах/отсутствии affordance.

---

### 🗃️ Commit Summary
- Внедрен модификаторный дроп, перенос логики поднятия/броска в аффордансы и очищение анимаций трубы.
