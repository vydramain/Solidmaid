# DEVLOG — Affordances, Ability Cleanup & Pipe Pass

---

## Date: 2025-11-18

### 🎯 Goal of the Day
> Выровнять архитектуру способностей/аффордансов, добавить трубе характер и исключить случайный бросок тяжёлых объектов.

---

### 🧩 Tasks Executed
- [x] Выделил новый overlap-пакет `affordances/` с базовым родителем и сценами для carriable/throwable, плюс импортировал Pipe как отдельный FPS объект.
- [x] Сделал общий родитель `ability.gd` похожим на новый `affordance.gd`: только имя/алиасы, сцена и зависимости; melee/throw/carry теперь описываются датой, а не enum’ами.
- [x] Ввёл аффордансы `carriable` и `throwable`, перенёс их в отдельные сцены/скрипты и заставил CarrySlots проверять теги перед подбором/броском.
- [x] Настроил Brick на двойной тег (можно брать и бросать), а Pipe оставил только carriable → трубу больше не получится швырять.
- [x] Добавил трубе placeholder-анимацию горизонтального замаха через AnimationPlayer, чтобы было что показать в melee прототипе.

---

### ⚙️ Issues / What Broke
- При рефакторе CarrySlots я перенёс логику выбора слота в одно место и сломал разделение «левая/правая рука»: сейчас предметы поднимаются и бросаются как попало, контроллер больше не понимает, какой слот свободен.
- Обновлённая логика affordance → pickup неожиданно блокирует трубу: предмет помечен как carriable, но из-за отвязанного CarrySlots/rig она не попадает в руки вовсе. Надо починить руки и проверку аффордансов вместе.

---

### 🔁 Next Steps / TODO
- [ ] Починить CarrySlots/контроллер, чтобы `hand_left`/`hand_right` снова управляли отдельными слотами и корректно реагировали на аффордансы.
- [ ] Завести полноценный пайплайн для Pipe: убедиться, что её можно поднять, проиграть swing-анимацию и ударить ею врага.
- [ ] Пройтись по другим предметам и добавить им явные affordance теги перед интеграцией enemies.

---

### 🗃️ Commit Summary (high-level)
- Refactor: унифицированный `ability.gd`, Ability scenes теперь задают поведение данными.
- Feature: Affordance система (carriable/throwable) + проверка в CarrySlots, brick/pipe обновлены под неё.
- Art/Feel: Placeholder swing-анимация трубы, чтобы melee тесты не выглядели пустыми.
